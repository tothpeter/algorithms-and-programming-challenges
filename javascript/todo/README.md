# Basic Todo App
This is a basic todo application, using only valilia js to build up all the functionality we need like renderring and upating the UI.

## How to run the test suite
- Start a HTTP server inside the `todo` directory
  - Then visit `localhost:8000/tests` in the browser
